import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#00B3AD",
    },
    secondary: {
      main: "#006663",
    },
    info: {
      main: "#FF6B1A",
    },
  },
});

export default theme;